# cs-dtw

This project performs dtw calculations on compute shaders. Place the input in the InputGesture folder and templates in templates folder. Current version does no support more input than template so keep the input either the same or less than number of templates.